[project]
name = "python-sandbox"
version = "0.1.0"
description = "Isolated Python runtime for Critjecture data analysis tools"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.39.3",
]
